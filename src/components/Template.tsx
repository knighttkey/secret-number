import React, {
  DragEvent,
  useRef,
  useState,
  MouseEvent,
  useEffect,
} from "react";
import "./../styles/Template.scss";

type Props = {};

export default (props: Props) => {
  const {} = props;
  return <div className="_container"></div>;
};
